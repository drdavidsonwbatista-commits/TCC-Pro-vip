- uses: actions/setup-node@v5
  with:
    node-version: 20
    cache: 'npm'
    cache-dependency-path: '**/package-lock.json' # ignora se não existir
  # ou simplesmente remova cache

- name: Install deps
  run: npm install --no-package-lock
